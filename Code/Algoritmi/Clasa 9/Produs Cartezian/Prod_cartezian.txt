  int n, m;
  cin >> n >> m;
  cout << "{";
  for (int i = 1; i <= n; i++)
  {
    for (int j = 1; j < m; j++)
    {
      cout << "(" << i << "," << j << "),";
    }
    if (i == n)
    {
      cout << "(" << i << "," << m << ")";
    }
    else
    {
      cout << "(" << i << "," << m << "),";
    }
  }
  cout << "}";

//Se dau 2 numere n, m. Pentru multimile A={1,2,...,n} si B={1,2,...,m} sa se afiseze A x B = {(a, b) | a∈A, b∈B}